extends root_layout 

block root_content 
  .center-screen 
    form.main-form(action="" method="POST")
      h2 Create a folder
      section 
        label(for="name") Name 
        input(type="text" name="name" required)
      if error
        .error= error.msg 
      button Create  
